import type { Metadata } from "next";
import { Source_Sans_3, Lato } from "next/font/google";
import "./globals.css";
import Providers from "@/provider/Provider";
import Script from "next/script";

// Source Sans 3 Font
const sourceSansPro = Source_Sans_3({
  weight: ["300", "400", "600", "700"],
  subsets: ["latin"],
  display: "swap",
  variable: "--font-source-sans", // Add a CSS variable for flexibility
});

// Lato Font
const lato = Lato({
  weight: ["300", "400", "700"],
  subsets: ["latin"],
  display: "swap",
  variable: "--font-lato", // Add a CSS variable for flexibility
});

export const metadata: Metadata = {
  title: "Trobits",
  description: "Generated by create next app",
};

// AdBanner Component
function AdBanner() {
  return (
    <>
      {/* Ad container */}
      <ins
        className="67a80a6f40c2d4901be49056"
        style={{ display: "inline-block", width: "120px", height: "600px" }}
      ></ins>

      {/* Script to load the ad */}
      <Script
        id="ad-banner-script-loader"
        strategy="afterInteractive"
        dangerouslySetInnerHTML={{
          __html: `
            !function(e,n,c,t,o,r,d){
              !function e(n,c,t,o,r,m,d,s,a){
                s=c.getElementsByTagName(t)[0],
                (a=c.createElement(t)).async=!0,
                a.src="https://"+r[m]+"/js/"+o+".js?v="+d,
                a.onerror=function(){a.remove(),(m+=1)>=r.length||e(n,c,t,o,r,m)},
                s.parentNode.insertBefore(a,s)
              }(window,document,"script","67a80a6f40c2d4901be49056",["cdn.bmcdn6.com"], 0, new Date().getTime())
            }();
          `,
        }}
      />
    </>
  );
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        {/* External Script */}
        <Script
          src="https://app.web3ads.net/main.js"
          strategy="afterInteractive"
          async
        />
      </head>
      <body
        className={`${sourceSansPro.variable} ${lato.variable} font-sans antialiased`}
      >
        {/* Google Analytics */}
        <Script
          async
          src="https://www.googletagmanager.com/gtag/js?id=G-101CZRBSL4"
        />
        <Script id="google-analytics">
          {`
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments);}
            gtag('js', new Date());
            gtag('config', 'G-101CZRBSL4');
          `}
        </Script>

        {/* Ad Banner */}
        <AdBanner />

        {/* Providers and Children */}
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}